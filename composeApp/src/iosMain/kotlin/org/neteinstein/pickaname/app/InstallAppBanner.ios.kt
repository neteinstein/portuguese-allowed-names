package org.neteinstein.pickaname.app

import androidx.compose.runtime.Composable

/** Nothing to install: this is a native app already (and it isn't the Android one). */
@Composable
actual fun InstallAppBanner() = Unit
